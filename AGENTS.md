# HoverIntentStudy 工作规则

## 目标
原生 iPadOS 研究采集工具。默认执行独立 V2 协议的 14 类任务，旧 Test 0–4 保留。姿势任选拇指或托握＋食指，均使用绑指 Pencil。任务与剩余次数始终清楚可见。

## 结构与命名
- `src/`：Xcode 工程、Swift 源码、可独立测试的 HoverStudyCore Swift Package。
- `docs/`：安装说明、实验配置、CSV 数据字典、技术与研究限制。
- `qa/`：自动检查脚本、验证记录、模拟器截图；模拟数据在文件名和数据字段明确标记。
- `qa/device-captures/`：真机 CSV 的检查快照，放入 `YYYY-MM-DD-HHMMSS/` 子目录；保留正式文件名和原始内容，不覆盖旧快照，不提交 Git。复制过程与应用写盘可能并发，检查发现不完整尾行时重新取快照，原证据保留。
- `.tmp/`：构建产物、测试输出和导出快照，仅可重建内容；不作为研究数据。
- `backups/`：卸载用户指定 App 前保存的设备容器，按 `YYYY-MM-DD/AppBundleID/` 命名；保留原文件及校验清单，不自动清理，不提交 Git。
- `data/`：用户指定保存到 Mac 的正式采集 CSV，保留 iPad 原文件名及原始字节；检查报告放 `qa/`。不自动清理、不提交 Git；再次拷贝遇到同名不同内容时先按时间归档旧文件，再保存新快照。
- `analysis/`：由 CSV 生成的轨迹回放和分析图，按 `trajectory_YYYY-MM-DD/` 分组；生成脚本放 `qa/`。保留来源哈希、方法和限制；不改写原 CSV，不自动清理。轨迹只连接相同输入来源、同一连续采集段中的实测点，超过 100 ms 或结束／重新开始时断线，不平滑或补造缺失数据；Z 使用 API 原值，不标毫米。默认分析描述笔尖行为；用户明确要求时可作离线探索性意图区分，必须区分任务指令标签与真实心理意图，不能把探索结果写回采集协议或原始 CSV。
- 手机仿真回放（含 Three.js 3D）仅使用手机局部范围 `0≤X≤390、0≤Y≤830` 内的 Pencil 样本；排除所有手指样本，保留其原始 CSV 证据。范围外或被排除的点必须打断轨迹，禁止过滤后跨越连接；受手指输入判失败的试次保留序号及排除原因但不进入汇总轨迹。接触点放在页面平面，悬停 Z 单独按显示比例映射，标明不是毫米；悬停 ENDED/CANCELLED 的位置保留为事件证据，不用结束回调的 Z 推断笔尖运动。旧回放先归档，再更新入口为手机仿真视图。
- 运动分析：在过滤后的同来源连续段内按实际单调时间差计算 XY 速度和加速度；导数时间放在支持区间中点，保留支持样本序号。速度变化率为切向加速度，与二维加速度幅值区别标注。Z 与 XY 分开，不合成物理三维加速度。按每次首个有效 Pencil TOUCH_DOWN 对齐；没有接触的试次不进入落笔对齐图，不跨段插值。仅曲线趋势可选 80 ms 段内中值显示，原坐标及导数导出不平滑。汇总按成功/失败、距离、尺寸筛选，并显示实际参与数量。
- 原始 PRD/PDF 和其他已有目录只读保留。运行时研究数据存于 iPad 应用 Documents/Data。

## 实现约束
- 正式采集仅目标 iPad、1366×1024 pt 全屏横向、真实 Pencil 输入；模拟输入不混入正式 CSV。
- 原始输入、界面刷新、实验状态、串行 CSV 写入分开；不以目标处停留时间决定指向成功。
- 每条记录保留任务、试次、条件、输入与时间来源。zOffset 不换算毫米。
- 失败/中断/跳过保留数据；重做保留旧 trialID 关联。保存失败必须停止推进。
- 导出源路径按解析符号链接后的文件系统路径校验，目录 URL 的尾斜杠及系统等价路径不影响有效性；禁止跨数据目录取源文件或覆盖源文件。导出复制／路径错误单独提示，不暂停采集；只有原始数据写盘失败进入保存恢复流程。
- 新行为先更新本规则/文档，再实现。密钥、签名身份不硬编码。
- 仅在用户明确要求同步或发布后执行 git push 或部署。用户明确要求后的设备安装按 README 步骤执行。

## 验证
采集应用修改后运行 Swift Package 核心测试、iPad 模拟器与设备无签名构建、CSV 检查。独立分析页面修改后检查脚本语法、过滤与断线规则、原 CSV 哈希，并在浏览器验证渲染及回放操作；页面统计使用“保留试次”，避免将保留的失败记录误称为有效实验。真机采集、15 分钟硬件可靠性和 AirDrop 必须实际完成才记 PASS。

## V2 协议（2026-09-15）
- `docs/protocol-v2.md` 是 V2 实现与验收依据，旧协议和 CSV 不重写。
- `src/App/StudyMedia/` 放离线场景媒体与来源清单；媒体使用稳定 ID、六条 20–40 秒视频，来源及许可证记录在 `docs/media-sources.md`；临时下载和转码放 `.tmp/`。保留已发布媒体版本，不自动清理。
- V2 核心文件使用 V2 前缀；采集、场景几何、状态机、渲染及写盘分离。共享对象几何用于命中、绘制和日志。
- V2 的 B1–B3 和 C 主动条件以连续 500 ms 悬停触发；B4 使用原始 XY 闭合圈选。上述例外不改变旧协议的无目标停留门槛。
- 自然阅读手指污染按时间段记录并排除分析，不抛弃整个试次。候选仅称“规则可能触发”，界面无选择或倒计时反馈。
- V2 回放可按悬停选择、菜单、候选、圈选闭合对齐，零触屏试次不能被丢弃。闭合边标记算法来源，不补造实测采样。

## 两轮／左右手轨迹分析
- 输出放 `analysis/trajectory_YYYY-MM-DD/hands_runN/`；报告、图、逐试次指标、质量清单和回放分别命名 `report.md`、`*.png`、`trial_metrics.csv`、`quality.json`、`replay-v2.html`。分析脚本放 `qa/`，原始 CSV 不改写。
- 左右手由原始标签或用户确认映射，缺少标签时使用第一轮／第二轮。姿势、先后顺序与左右手不能互相替代；单人各一轮只作描述比较。
- 比较相同任务／条件／场景，几何任务优先配对距离、尺寸和方向。自然阅读与短段阅读分别报告；失败记录保留并报告，手指导致失败的短试次不纳入汇总轨迹。
- 本轮指标限定 TRIAL_START 至 TRIAL_END；试次间隔中仍被标为前一试次的采样另计 `outsideTrial`，原 CSV 保留，不能计入操作路径或导数。
- 导数保留全部合法连续实测边，并另行报告极短时间差的数量；主要运动分布以时间加权统计，短于 1 ms 的边仅从主要导数汇总排除，记录规则与数量，不删除坐标或补点。速度／加速度以连续段和支持时间为依据。
- 逐场景候选率以自然段总记录时长为分母；同时报告有效悬停时间、缺口、污染和覆盖，禁止把候选解释为真实误触。阅读轨迹使用页面屏幕坐标，滚动时不把屏幕停留当成同一内容对象停留。

## 用户标注与探索性意图区分
- 用户确认两轮均右手：第一轮拇指、第二轮托握＋食指。将确认来源写入派生 metadata，原日志姿势及指令保留。
- 用户说明 B1 一律理解为选 B：只有完成 MENU_OPEN 且 MENU_SELECT 为 B、原错误仅 WRONG_MENU_ITEM 的记录可覆核为动作完成；保留 rawSuccess/rawError、派生 success 及覆盖理由，不更改其他错误。
- 本次新增输出仍在 hands_run2：`analysis-annotations.json`、`intent_episodes.csv`、`intent_exploration.json`、`intent_features.png`。更正前的小型报告与指标保存在 `revisions/pre-clarification/`，不复制可重建的巨型回放数据。目录不自动清理。
- 主动与自然阅读的探索比较使用 C3 同场景的首次 500 ms 悬停窗，只选同一对象、同一连续 Pencil 段内覆盖至少 450 ms、至少 12 实测点的窗口。不使用确认后的样本、任务／条件标签、事件类别作预测特征。
- 按整轮跨姿势训练／测试，报告类别数、混淆计数、场景分布与 AUC；不随机拆分相邻点或同一试次。自然候选是指令基线标签，不是逐段确认的真实无意图。单人两轮不能验证通用意图识别。
- B4 单独识别闭合圈选动作，不与停留选择混作同一个静态 Hover 分类器。

## 行动阶段与 Hover 多选图
- 本次补充仍放 hands_run2：`motion-study.html`、`motion-study.md`、`motion_phases.csv`、`multiselect_metrics.csv`、`motion-study.json`、`multiselect_paths.png`、`multiselect_motion.png`、`selection_alignment.png`、`pointing_motion.png`。脚本放 qa，不覆盖原始 CSV 或已有报告内容。
- 加速／减速用有符号速度变化率 Δ|v|/Δt，正为加速、负为减速；二维加速度幅值没有正负，不能用其幅值区分减速。只在同一原始连续段的相邻合法边计算，支持边均至少 1 ms，保留支持序号及时间。
- 可在同一连续段内显示 80 ms 移动中位趋势，必须同时显示原始导数、明确标注趋势用途；坐标不平滑、不插值，缺口处断线。
- B3 按实际 HOVER_SELECT 顺序编号三个对象，图显示目标几何、实测路径、最终有效 500 ms 停留段及事件时刻；完整过程中的提前离开／重试保留。B4 空中圈选作为另一类多目标操作分别说明。
- 进目标前 300 ms 与有效停留前 500 ms 的速度比较，只使用同一连续悬停段，前窗至少 200 ms、后窗至少 450 ms 覆盖；无法满足的记录明确排除，不把缺口当作减速。结论报告满足条件的数量及每姿势分布。

## A/B 行为特征发现与 C 回顾性检验
- 本次整体分析独立输出于 hands_run2：`behavior-overview.html/md/json`、`behavior_task_features.csv`、`target_hover_features.csv`、`behavior_windows.csv`、`behavior_feature_atlas.png`、`behavior_signatures.png`、`behavior_phase_curves.png`、`ab_discovery_c_check.png`、`behavior_validation.json`。生成脚本与检查放 qa。已有分析保留，入口追加链接；不改原协议或 CSV。
- 整体阶段曲线与路径图使用相同、可追溯的代表试次；显示原始速度和有符号速度变化率、同段 80 ms 中值趋势、菜单／驻留／圈选事件。B1/B2 可叠加同几何 A1 示例，不能把示例曲线当整体统计；缺口保留，B3/B4 阅读对照不称几何匹配。
- A1–A4 抽象操作、A5–A7 三种自然阅读分别作为基线，不合并不同任务时长的最大速度或路径长度。阶段指标限定目标呈现后的实测段，手指、手机外和缺口仍断开；接触后证据不能作为接触前意图预测特征。
- B1=悬停唤菜单后点击，B2=纯悬停指向／单选，B3=逐个悬停多选，B4=空中圈选。把协议强制的 500 ms、三个对象及闭合要求与自发运动差异区别报告。B3/B4 没有相同几何的 A 多点操作对照，不能宣称已证明其意图特征唯一。
- 小目标内预接触连续悬停按实际圆形绘制几何判定，保留零进入／覆盖不足；B1 菜单出现前、B2/B3 选中前限定相同阶段。先前起点稳定不得计入目标停留。
- 圈线候选从全部任务实测连续悬停段独立扫描，使用冻结的几何门槛：0.5–3 s 窗、至少 12 点、闭合距离≤20 pt、离起点≥40 pt、路径≥120 pt、面积≥2000 pt²；起点每 100 ms 从实测点选取，不插值、不跨段、闭合边为算法边。报告候选试次及可观测悬停时长，不能把滑窗候选称为正确圈选或推断真实意图。
- 用 A 自然阅读达标候选与 B1–B3 达标窗发现静态 XY 特征；训练仅 A/B，固定速度中位数／位置 RMS、正则强度和阈值，测试 C3，不用任务／条件／事件名作模型输入。C1/C2 另行检查完成数量、短窗及接触混淆，不补造正例。
- C 曾用于探索，当前 C 检验必须称回顾性留组检验，不能称未见过数据的独立验证。给出域偏移、每场景误报、训练试次数及测试覆盖。C1–C3 验证的是同场景条件区分；没有 B3/B4 混淆条件，不能代替多选／圈选的专项验证。

## Home position 候选与过滤探索
- 在 hands_run2 新增 `home-position.html/md/json`、`home_candidates.csv`、`home_filter_windows.csv`、`home_position_map.png`、`home_filter_tradeoff.png`、`home_acquisition.png`、`home_validation.json`；脚本和测试放 qa。仅离线分析，不改采集规则、旧报告结论或原 CSV；已有入口追加链接。
- 强制起点、手机内热点和自然休息位置分别命名。自然候选模型只用 A5–A7 完整基线前 60 秒、每姿势独立学习；后 60 秒检查位置稳定性，B/C 不参与估计。另报告同姿势跨场景差异，不自动假设只有一个固定 home。
- 以连续实测悬停段中的非重叠 500 ms 窗估计局部低运动区，至少 12 点、450 ms 覆盖，XY 位置 RMS≤15 pt 且速度中位≤100 pt/s。中心为时间加权统计位置；密度统计可以估计分布，不能将统计中心或平滑密度伪装为实测轨迹。窗口不跨缺口，不用目标／选择标签决定 home。
- 主方案固定圆半径 50 pt，另将 30／80 pt 作为敏感性检查，禁止在 C 结果上选最佳半径。最密集候选簇按窗口覆盖时间计权；跨场景模型每场景同权。训练至少 6 窗，后半段至少 6 窗且估计中心相距≤50 pt才标位置复现；仅热点不能确认心理休息状态。
- 过滤模拟仅针对静态选择窗口：同一窗在候选 home 内覆盖时间≥80%，同时满足上述低运动条件才降低触发；报告自然候选减少量及主动窗口误过滤，保留 C 已探索的回顾性限制。不删除数据、截断圈线或硬拒绝 home 内所有操作；没有足够 home 证据时报告缺失。
- 获取时间按 2／5／10／15／20／30／45／60 秒逐前缀估计，每前缀只用已收到且完整结束的窗口。主簇至少 6 窗才记初步候选；至少两个有新增窗口的检查点、中心差≤20 pt 才记在线暂稳，后续稳定时间与后半段复现单独标为回顾性判断，不能承诺固定时长。主过滤模拟只信任前后半段各有 6 个主簇窗且中心差≤50 pt 的同场景模型，其余不触发位置过滤；B1–B3 仅作跨场景迁移检查，B4 不裁剪。
- 同一段 A 自然阅读另做前60秒估计→后60秒候选抑制检查，仅按训练主簇足够决定可用性，不用后60秒选择模型；该检查只有自然条件，不报告主动识别性能。C3 另报告估计至应用的实际时间间隔，区分同段短期位置参考与跨任务后的迁移。

## V2.2 采集修订与统一报告
- 新会话使用 HOVER_INTENT_V2_2，旧无 revision 配置按 V2.1 解码，旧记录和未结束旧会话不改写。docs/protocol-v2-1.md 保留旧规范，docs/protocol-v2.md 为当前规范。
- A1–A7/C1–C3 按有效计划题推进；失败/跳过当前尝试自动重试同一条件和几何，新 trialID 关联前次。B1–B4 默认各12题，成功/失败均推进，独立配置配平套数。保留所有尝试，统计区分唯一有效题、首次成功率和全部尝试成功率。
- C1 使用24/36/48pt，C2 36pt标记且起点与标记分离，C3 48pt具体配图/收藏对象；两条件共享对象，命中、显示、日志共用边界。自然 C3 的候选只针对同一小对象，A 阅读候选范围保留。
- 统一报告放 hands_run2/study-report.html/md/json，由 qa/build_study_report.py 从完整派生统计与原数据来源哈希生成；纳入质量、轨迹、加减速、菜单、多选、圈选、C回顾性检验和home位置结果。旧报告只追加入口链接，不覆盖正文。旧数据结论与新协议实现状态分别展示。

## GitHub 公开发布（2026-09-15 用户授权）
- 用户明确要求将应用和报告发布到 GitHub，名称 Intentional Hover Study；本次允许创建 purryc/Intentional-Hover-Study 公开仓库、提交与推送、GitHub Pages 发布及源码/报告版本发布。后续推送仍需用户明确要求。
- `site/`：公开站点入口、`report/` 的统一报告与17张分析图、`replay/` 的本地CSV导入工具。公开站点不内嵌正式逐点轨迹；导入在浏览器内完成，不上传数据。生成脚本为 `qa/build_public_site.py`；先检查来源哈希，公开结果JSON只保留汇总及模型参数，不含设备身份、原始点列或个人签名。
- `.github/workflows/`：公开网站部署及源码检查。Pages 仅部署 `site/`，不部署工作区或 data/。凭证只用 GitHub 内置令牌。
- `qa/vendor/`：供可独立运行的回放使用的 Three.js 与 OrbitControls，保留原始许可证和版本信息；不改第三方源码。公开与本地构建共用此副本。
- Git 按路径白名单添加 src/、docs/、qa/源码及明确列出的公开验收记录、site/、工作流。analysis/、原始CSV、设备检查JSON、运行日志、构建缓存、设备截图附件与备份留在本地，不自动清理。
- `docs/publishing.md` 说明发布内容、复现条件与数据可用性；`qa/verification-public.md` 记录已完成检查与未完成真机专项验证，不含设备UUID或本机路径。公开仓库提供Xcode源码安装方式，不发布个人签名的IPA。
- 发布前审查 Git 暂存清单、站点本地链接、媒体归属、个人信息与文件大小；发布后核对远端提交、Pages部署结果和公开网页。原CSV字节保持不变。
